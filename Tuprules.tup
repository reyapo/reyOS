export reyOS
export BUILD_ROOT
export PREFIX
export TARGET
export PATH
export ARCH
export CC
export CPP
export AR

export include

!cpp = |> ^ CPP %o^ $CPP |>
!cc = |> ^ CC %o^ $CC |>
!nasm = |> ^ nasm %o^ nasm |>
!ar = |> ^ AR %o^ $AR |>